type: object
properties:
  id:
    type: integer
  title:
    type: string
  description:
    type: string
  price_cents:
    type: integer
  currency:
    type: string
    enum:
      - USD
    example: USD
    description: Only USD is supported (single-currency).
  durationMinutes:
    type: integer
    minimum: 15
    maximum: 240
  isActive:
    type: boolean
  influencerTimezone:
    type: string
    description: |
      IANA timezone identifier for the influencer (e.g., "America/New_York").
      All date parameters should be interpreted in this timezone. All slot times returned by the API are in UTC; frontend is responsible for converting to the customer's local timezone.
    example: 'America/New_York'
  workingHours:
    type: object
    description: >-
      Working hours configuration for each day of the week. Defaults to 9 AM - 5
      PM Monday-Friday if not set. Times are stored as the influencer configured them (typically in their local timezone); frontend should interpret based on the influencer's timezone preference.
    properties:
      monday:
        type: object
        properties:
          enabled:
            type: boolean
          start:
            type: string
            format: time
            nullable: true
          end:
            type: string
            format: time
            nullable: true
      tuesday:
        type: object
        properties:
          enabled:
            type: boolean
          start:
            type: string
            format: time
            nullable: true
          end:
            type: string
            format: time
            nullable: true
      wednesday:
        type: object
        properties:
          enabled:
            type: boolean
          start:
            type: string
            format: time
            nullable: true
          end:
            type: string
            format: time
            nullable: true
      thursday:
        type: object
        properties:
          enabled:
            type: boolean
          start:
            type: string
            format: time
            nullable: true
          end:
            type: string
            format: time
            nullable: true
      friday:
        type: object
        properties:
          enabled:
            type: boolean
          start:
            type: string
            format: time
            nullable: true
          end:
            type: string
            format: time
            nullable: true
      saturday:
        type: object
        properties:
          enabled:
            type: boolean
          start:
            type: string
            format: time
            nullable: true
          end:
            type: string
            format: time
            nullable: true
      sunday:
        type: object
        properties:
          enabled:
            type: boolean
          start:
            type: string
            format: time
            nullable: true
          end:
            type: string
            format: time
            nullable: true
