type: object
required:
  - newPassword
  - newPassword_confirmation
properties:
  currentPassword:
    type: string
    description: Required if user has a password
    nullable: true
  newPassword:
    type: string
    minLength: 8
  newPassword_confirmation:
    type: string
