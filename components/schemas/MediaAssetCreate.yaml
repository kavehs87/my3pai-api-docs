type: object
required:
  - title
  - description
  - type
  - price_cents
  - currency
properties:
  title:
    type: string
    maxLength: 255
  description:
    type: string
    maxLength: 1000
  type:
    type: string
    enum:
      - Video
      - Photo
      - Preset
      - Audio
      - Template
      - Other
  price_cents:
    type: integer
    minimum: 0
  currency:
    type: string
    enum:
      - USD
    example: USD
    description: Only USD is supported (single-currency).
  isActive:
    type: boolean
