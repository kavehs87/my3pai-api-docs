allOf:
  - $ref: './AdminPayment.yaml'
  - type: object
    properties:
      refunds:
        type: array
        items:
          type: object
          properties:
            id:
              type: integer
            amount_cents:
              type: integer
            reason:
              type: string
            status:
              type: string
              enum: [pending, processed, failed]
            processed_at:
              type: string
              format: date-time
              nullable: true
      stripe_charge_id:
        type: string
        nullable: true
      stripe_balance_transaction_id:
        type: string
        nullable: true
