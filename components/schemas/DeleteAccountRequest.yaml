type: object
properties:
  password:
    type: string
    description: User password. Used for verification if the user has a password set.
    nullable: true
  providerReauthToken:
    type: string
    description: Google ID token. Used for verification if the user is an OAuth user.
    nullable: true
