type: object
properties:
  currency:
    type: string
    enum:
      - USD
      - EUR
      - GBP
      - JPY
  language:
    type: string
    enum:
      - en
      - es
      - fr
      - de
  timezone:
    type: string
  country:
    type: string
    nullable: true
    description: ISO 3166-1 alpha-2 country code (2 characters)
    pattern: "^[A-Z]{2}$"
    example: "US"
  notifications:
    type: object
    properties:
      email:
        type: boolean
      push:
        type: boolean
      marketing:
        type: boolean
  wants_to_become_influencer:
    type: boolean
    deprecated: true
    description: Deprecated 2026-08-27. Use POST /influencer/apply and GET /me.influencer_application. Read-only alias (true only when application approved or role=influencer). Writes 422.
  dark_theme:
    type: boolean
