post:
  tags:
    - Admin Payouts
  summary: Requeue failed payout
  description: Mark a failed payout as pending for re-processing. Status management only — no money movement is performed. Only payouts with status 'failed' can be requeued.
  parameters:
    - name: id
      in: path
      required: true
      schema:
        type: integer
  responses:
    '200':
      description: Payout marked for re-processing
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
              data:
                type: object
                properties:
                  id:
                    type: integer
                  status:
                    type: string
    '401':
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
    '403':
      description: Forbidden - not admin
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
    '404':
      description: Payout not found
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
    '422':
      description: Validation error - payout status is not 'failed'
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
