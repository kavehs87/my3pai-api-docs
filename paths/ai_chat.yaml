post:
  tags:
    - AI
  summary: Chat with the AI model
  description: >
    Send a message to the AI model and receive a generated text response.
    Runs on OpenRouter using the configured model (default: the Free Models Router,
    "openrouter/free"; pin a specific model with the AI_MODEL environment variable).
    
    **Authentication:**
    Requires valid Sanctum authentication token.
    
    **Rate Limiting:**
    Subject to standard API rate limiting.
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              description: The user's message/prompt to send to the AI model
              maxLength: 10000
              example: "What are the best travel destinations in Europe?"
  responses:
    '200':
      description: AI response generated successfully
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: true
              data:
                type: object
                properties:
                  message:
                    type: string
                    description: The generated text response from the AI model
                    example: "Europe offers a diverse range of incredible travel destinations..."
    '422':
      description: Validation failed
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: false
              message:
                type: string
                example: Validation failed
              errors:
                type: object
                properties:
                  message:
                    type: array
                    items:
                      type: string
                    example:
                      - "The message field is required."
    '500':
      description: Server error or OpenRouter API error
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: false
              message:
                type: string
                example: Failed to generate response
              error:
                type: string
                description: Error message (only shown in local/development environments)
                example: "OpenRouter API error (401): API key is invalid or missing"

