get:
  tags:
    - Public
    - Masterclasses
  summary: Get public masterclasses for an influencer
  description: Get paginated list of published masterclasses for an influencer
  security: []
  parameters:
    - name: username
      in: path
      required: true
      schema:
        type: string
    - name: page
      in: query
      schema:
        type: integer
        default: 1
        minimum: 1
    - name: per_page
      in: query
      schema:
        type: integer
        default: 10
        maximum: 50
        minimum: 1
  responses:
    '200':
      description: List of masterclasses
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
              data:
                type: array
                items:
                  $ref: ../components/schemas/MasterclassPublic.yaml
              meta:
                $ref: ../components/schemas/PaginationMeta.yaml
    '404':
      description: Influencer not found
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
