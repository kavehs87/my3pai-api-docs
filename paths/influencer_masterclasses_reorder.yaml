put:
  tags:
    - Influencers
    - Masterclasses
  summary: Reorder masterclasses
  description: Reorder masterclasses by providing an array of IDs in desired order
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          required:
            - ids
          properties:
            ids:
              type: array
              items:
                type: integer
  responses:
    '200':
      description: Masterclasses reordered
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
              message:
                type: string
    '404':
      description: Some masterclasses not found or do not belong to user
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ErrorResponse.yaml
    '422':
      description: Validation failed
      content:
        application/json:
          schema:
            $ref: ../components/schemas/ValidationError.yaml
